apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  python-api.json: |
{{ .Files.Get "dashboards/python-api.json" | indent 4 }}
